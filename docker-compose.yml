version: '3.8'

services:
  chat-agent:
    build: .
    env_file: .env
    volumes:
      - ./session:/app/session
    restart: unless-stopped
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
